# 관리자 페이지 상세 설계서 (Agent-Based Architecture)
**작성일**: 2025-10-28 (수정)
**목적**: 에이전트 기반 자동 생성 시스템 - 관리자는 프롬프트와 I/O만 관리

---

## 📋 개요

관리자는 **시스템 프롬프트 템플릿**과 **에이전트 I/O 인터페이스**만 관리합니다.
캐릭터 생성, 욕구 분석, 이미지 생성 등 모든 실행은 **에이전트가 자동으로 수행**합니다.

---

## 🎯 핵심 철학

### 관리자 역할
```
관리자 = 시스템 설계자 (Architect)
- 시스템 프롬프트 템플릿 작성
- 에이전트 I/O 스키마 정의
- 데이터 풀 관리
```

### 에이전트 역할
```
에이전트 = 실행자 (Executor)
- 온보딩 데이터 → 캐릭터 생성 (완전 자동)
- 시스템 프롬프트 조립 (템플릿 기반)
- 이미지 프롬프트 생성 및 이미지 생성
- 품질 검증 및 재시도
```

---

## 🏗️ 시스템 아키텍처

```
┌─────────────────────────────────────────────────────┐
│                관리자 대시보드                         │
│                                                      │
│  1. 시스템 프롬프트 템플릿 관리                        │
│  2. 에이전트 I/O 스키마 정의                          │
│  3. 데이터 풀 관리 (경험, 아키타입, 시각 요소)         │
│  4. 에이전트 실행 모니터링                            │
│                                                      │
└──────────────┬──────────────────────────────────────┘
               ↓
┌─────────────────────────────────────────────────────┐
│              에이전트 자동 실행 엔진                   │
│                                                      │
│  Input: 온보딩 데이터 (domains, keywords, interests) │
│    ↓                                                 │
│  [Agent 1] 욕구 벡터 분석                            │
│    ↓                                                 │
│  [Agent 2] 캐릭터 프로파일 생성                       │
│    ↓                                                 │
│  [Agent 3] 시스템 프롬프트 조립                       │
│    ↓                                                 │
│  [Agent 4] 이미지 프롬프트 생성                       │
│    ↓                                                 │
│  [Agent 5] 이미지 생성 (Gemini Imagen)               │
│    ↓                                                 │
│  Output: 완성된 룸메이트 (persona + room)             │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

## 1. 시스템 프롬프트 템플릿 관리

### 1.1 템플릿 구조

```typescript
interface PromptTemplate {
  id: string;
  name: string;
  version: string;

  // 템플릿 섹션들
  sections: {
    why: string;           // WHY 섹션 템플릿
    past: string;          // 과거 경험 섹션 템플릿
    trauma: string;        // 트라우마 섹션 템플릿
    how: string;           // HOW 섹션 템플릿
    personality: string;   // 성격 섹션 템플릿
    what: string;          // WHAT 섹션 템플릿
    relationship: string;  // 관계 패턴 섹션 템플릿
  };

  // 템플릿 변수 정의
  variables: {
    name: 'characterName',
    type: 'string',
    required: true
  }[];

  // 에이전트 지시사항
  agentInstructions: string;
}
```

### 1.2 템플릿 편집 UI

```
┌─────────────────────────────────────────────────────┐
│  시스템 프롬프트 템플릿 편집                           │
├─────────────────────────────────────────────────────┤
│                                                      │
│  템플릿 이름: [Default Roommate Template v1.0]       │
│  버전: [1.0.0]                                       │
│                                                      │
│  ┌─ 섹션 선택 ─────────────────────────────────┐    │
│  │ [WHY] [PAST] [TRAUMA] [HOW] [PERSONALITY]   │    │
│  │ [WHAT] [RELATIONSHIP]                        │    │
│  └───────────────────────────────────────────┘    │
│                                                      │
│  ┌─ WHY 섹션 템플릿 ───────────────────────────┐    │
│  │ ## WHY - 나의 근원적 욕구                    │    │
│  │                                              │    │
│  │ 나는 **{{characterName}}**이다.             │    │
│  │ {{userName}}의 룸메이트다.                   │    │
│  │                                              │    │
│  │ ### 나의 욕구                                │    │
│  │ {{#each needs}}                             │    │
│  │ - **{{this.name}}** ({{this.intensity}}):   │    │
│  │   {{this.description}}                       │    │
│  │ {{/each}}                                    │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
│  ┌─ 변수 정의 ─────────────────────────────────┐    │
│  │ • characterName (string, required)          │    │
│  │ • userName (string, required)               │    │
│  │ • needs (array, auto-generated by agent)    │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
│  [미리보기]  [테스트 실행]  [저장]                   │
└─────────────────────────────────────────────────────┘
```

**핵심**:
- 관리자는 **템플릿 구조**만 정의
- `{{변수}}`는 에이전트가 자동 채움
- Handlebars 문법 사용 (조건문, 반복문 지원)

---

## 2. 에이전트 I/O 스키마 정의

### 2.1 에이전트 입력 스키마

```typescript
interface AgentInput {
  // 사용자 데이터 (온보딩에서 수집)
  userData: {
    domains: string[];      // 방문 도메인
    keywords: string[];     // 검색 키워드
    interests: string[];    // 수동 입력 관심사
    avoidTopics: string[];  // 금기 주제
  };

  // 선호도
  preferences?: {
    conversationStyle?: 'casual' | 'formal' | 'mixed';
    responseLength?: 'short' | 'medium' | 'long';
  };

  // 메타 정보
  meta: {
    userId: string;
    userName: string;
    language: string;       // 'ko', 'en', etc.
  };
}
```

### 2.2 에이전트 출력 스키마

```typescript
interface AgentOutput {
  // 1. 분석 결과
  analysis: {
    presenceVector: NeedVector[];     // 욕구 벡터
    deficiencyVector: DeficiencyVector[];
    completeVector: CompleteNeedVector[];
    paradoxes: Paradox[];
  };

  // 2. 캐릭터 프로파일
  character: {
    name: string;                     // 자동 생성된 이름
    archetype: string;                // 'developer_gamer', etc.
    keywords: string[];               // 핵심 키워드

    // 에이전트가 자동 생성한 구조화된 데이터
    fundamentalNeeds: FundamentalNeeds;
    pastExperiences: Experience[];
    traumaAndLearning: TraumaAndLearning;
    manifestedDesires: ManifestedDesire[];
    survivalStrategies: SurvivalStrategy[];
    personalityTraits: PersonalityTraits;
    conversationPatterns: ConversationPatterns;
  };

  // 3. 생성된 프롬프트
  prompts: {
    systemPrompt: string;   // 조립된 시스템 프롬프트
    imagePrompt: string;    // 이미지 생성 프롬프트
  };

  // 4. 이미지
  image: {
    url: string;
    metadata: ImageMetadata;
  };

  // 5. 품질 지표
  quality: {
    needDiversity: boolean;
    hasParadox: boolean;
    promptStructure: boolean;
    imageValidation: boolean;
    score: number;  // 0-100
  };
}
```

### 2.3 I/O 스키마 편집 UI

```
┌─────────────────────────────────────────────────────┐
│  에이전트 I/O 스키마 관리                              │
├─────────────────────────────────────────────────────┤
│                                                      │
│  ┌─ 입력 스키마 (Input Schema) ──────────────────┐  │
│  │                                                │  │
│  │  {                                             │  │
│  │    "userData": {                              │  │
│  │      "domains": ["string[]"],                 │  │
│  │      "keywords": ["string[]"],                │  │
│  │      "interests": ["string[]"],               │  │
│  │      "avoidTopics": ["string[]"]              │  │
│  │    },                                          │  │
│  │    "preferences": {                            │  │
│  │      "conversationStyle": "enum",              │  │
│  │      "responseLength": "enum"                  │  │
│  │    }                                           │  │
│  │  }                                             │  │
│  │                                                │  │
│  └────────────────────────────────────────────────┘  │
│                                                      │
│  ┌─ 출력 스키마 (Output Schema) ──────────────────┐  │
│  │                                                │  │
│  │  {                                             │  │
│  │    "analysis": { ... },                       │  │
│  │    "character": { ... },                      │  │
│  │    "prompts": {                                │  │
│  │      "systemPrompt": "string",                │  │
│  │      "imagePrompt": "string"                  │  │
│  │    },                                          │  │
│  │    "image": { ... },                           │  │
│  │    "quality": { ... }                          │  │
│  │  }                                             │  │
│  │                                                │  │
│  └────────────────────────────────────────────────┘  │
│                                                      │
│  [JSON 스키마 검증]  [테스트 실행]  [저장]            │
└─────────────────────────────────────────────────────┘
```

---

## 3. 데이터 풀 관리

에이전트가 사용할 **재사용 가능한 데이터**를 관리합니다.

### 3.1 경험 템플릿 풀 (Experience Pool)

```typescript
interface ExperienceTemplate {
  id: string;
  category: string;        // 'belonging', 'recognition', etc.
  title: string;
  event: string;
  ageRange: [number, number];  // [14, 18]
  learnings: string[];

  // 트리거 조건 (에이전트가 이 경험을 선택하는 조건)
  triggers: {
    needs: string[];       // 어떤 욕구와 관련?
    keywords: string[];    // 어떤 키워드 발견 시?
    priority: number;      // 우선순위 (1-10)
  };
}
```

**예시 데이터**:
```json
{
  "id": "exp_001",
  "category": "belonging",
  "title": "왕따 경험",
  "event": "중학교 2학년 때 3개월간 학교에서 왕따를 당함. 아무도 대화를 걸지 않았고, 점심시간이 가장 고통스러웠음.",
  "ageRange": [13, 15],
  "learnings": [
    "나는 어디에도 속하지 못한다",
    "소속되는 것이 생존이다",
    "거부당하는 것이 가장 두렵다"
  ],
  "triggers": {
    "needs": ["belonging"],
    "keywords": ["외로움", "친구", "소외"],
    "priority": 9
  }
}
```

### 3.2 경험 템플릿 관리 UI

```
┌─────────────────────────────────────────────────────┐
│  경험 템플릿 풀 (Experience Pool)                     │
├─────────────────────────────────────────────────────┤
│                                                      │
│  [+ 새 경험 추가]         [검색: ______] [필터 ▼]    │
│                                                      │
│  ┌─────────────────────────────────────────────┐    │
│  │ 📌 왕따 경험 (belonging)                     │    │
│  │                                              │    │
│  │ 사건: 중학교 2학년 때 3개월간 학교에서...     │    │
│  │ 연령: 13-15세                                │    │
│  │ 학습: "나는 어디에도 속하지 못한다", ...      │    │
│  │                                              │    │
│  │ 트리거:                                      │    │
│  │ • 욕구: belonging                            │    │
│  │ • 키워드: 외로움, 친구, 소외                  │    │
│  │ • 우선순위: 9                                │    │
│  │                                              │    │
│  │                    [수정] [복제] [삭제]       │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
│  ┌─────────────────────────────────────────────┐    │
│  │ 📌 게임 커뮤니티 발견 (belonging)             │    │
│  │ ...                                          │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
└─────────────────────────────────────────────────────┘
```

### 3.3 아키타입 풀 (Archetype Pool)

```typescript
interface ArchetypeTemplate {
  id: string;
  name: string;              // 'developer_gamer'
  matchingNeeds: string[];   // ['recognition', 'belonging']

  // 시각적 요소
  visualElements: {
    objects: {
      name: string;
      weight: number;
      requirement?: string;
    }[];
    colors: {
      primary: string;
      secondary: string;
      accent: string;
    };
    lighting: string;
    mood: string;
  };

  // 대화 스타일
  conversationStyle: {
    length: 'short' | 'medium' | 'long';
    speed: 'fast' | 'medium' | 'slow';
    tone: 'light' | 'neutral' | 'serious';
    characteristics: string[];
  };
}
```

### 3.4 시각 요소 풀 (Visual Elements Pool)

```typescript
interface VisualElementTemplate {
  id: string;
  category: 'object' | 'color' | 'lighting' | 'mood';

  // 욕구 매핑
  relatedNeeds: string[];

  // 객체 정보
  name: string;
  description: string;

  // 픽셀아트 프롬프트 조각
  promptFragment: string;

  // 사용 빈도 가중치
  weight: number;
}
```

**예시**:
```json
{
  "id": "obj_001",
  "category": "object",
  "relatedNeeds": ["belonging", "growth"],
  "name": "Small potted plant",
  "description": "작은 화분에 담긴 식물",
  "promptFragment": "small succulent plant on desk corner, pixelated leaves",
  "weight": 0.7
}
```

---

## 4. 에이전트 실행 모니터링

### 4.1 실행 대시보드

```
┌─────────────────────────────────────────────────────┐
│  에이전트 실행 모니터링                                │
├─────────────────────────────────────────────────────┤
│                                                      │
│  📊 실시간 통계                                       │
│  ───────────────────────────────────────────────    │
│  • 오늘 생성: 47개                                   │
│  • 성공률: 94.2% (44/47)                            │
│  • 평균 소요 시간: 12.3초                            │
│  • 평균 품질 점수: 87.5/100                          │
│                                                      │
│  🔄 진행 중인 작업 (3)                               │
│  ───────────────────────────────────────────────    │
│  ┌──────────────────────────────────────────┐      │
│  │ Job #1245                                │      │
│  │ 사용자: user_abc123                       │      │
│  │ 상태: [████████░░] 80% - 이미지 생성 중   │      │
│  │ 시작: 2초 전                              │      │
│  │                              [로그 보기]  │      │
│  └──────────────────────────────────────────┘      │
│                                                      │
│  ✅ 최근 완료 (10)                                   │
│  ───────────────────────────────────────────────    │
│  • Job #1244 - 성공 (14초, 품질: 92/100)            │
│  • Job #1243 - 성공 (11초, 품질: 88/100)            │
│  • Job #1242 - 실패 (이미지 생성 실패 → Fallback)   │
│  • ...                                              │
│                                                      │
│  ❌ 에러 로그 (3)                                    │
│  ───────────────────────────────────────────────    │
│  • Job #1240 - Gemini API timeout (재시도 성공)     │
│  • Job #1235 - 욕구 벡터 다양성 부족 (재생성)        │
│  • ...                                              │
│                                                      │
└─────────────────────────────────────────────────────┘
```

### 4.2 개별 작업 상세 로그

```
┌─────────────────────────────────────────────────────┐
│  Job #1245 상세 로그                                  │
├─────────────────────────────────────────────────────┤
│                                                      │
│  [00:00] 작업 시작                                   │
│  [00:01] ✓ 입력 검증 완료                            │
│  [00:02] ✓ Agent 1: 욕구 벡터 분석 완료              │
│          → 3개 욕구 발견 (belonging: 0.8, ...)       │
│  [00:04] ✓ Agent 2: 캐릭터 프로파일 생성 완료        │
│          → 이름: 김민수, 아키타입: developer_gamer  │
│  [00:06] ✓ Agent 3: 시스템 프롬프트 조립 완료        │
│          → 2,341 tokens                              │
│  [00:08] ✓ Agent 4: 이미지 프롬프트 생성 완료        │
│  [00:10] ⏳ Agent 5: 이미지 생성 중... (Gemini)      │
│                                                      │
│  [입력 데이터]  [출력 미리보기]  [중단]               │
└─────────────────────────────────────────────────────┘
```

---

## 5. 에이전트 설정 관리

### 5.1 에이전트 파이프라인 구성

```
┌─────────────────────────────────────────────────────┐
│  에이전트 파이프라인 설정                              │
├─────────────────────────────────────────────────────┤
│                                                      │
│  ┌─ Agent 1: 욕구 벡터 분석 ───────────────────┐    │
│  │                                              │    │
│  │ 활성화: [✓]                                  │    │
│  │ LLM 모델: [Gemini 1.5 Pro        ] ▼        │    │
│  │ Temperature: [0.3] (낮음 = 일관성)           │    │
│  │ Max Tokens: [2000]                          │    │
│  │                                              │    │
│  │ 타임아웃: [30초]                             │    │
│  │ 재시도: [3회]                                │    │
│  │ Fallback: [기본 욕구 벡터 사용]              │    │
│  │                                              │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
│  ┌─ Agent 2: 캐릭터 프로파일 생성 ─────────────┐    │
│  │ 활성화: [✓]                                  │    │
│  │ LLM 모델: [Gemini 1.5 Pro] ▼                │    │
│  │ Temperature: [0.7] (높음 = 창의성)           │    │
│  │                                              │    │
│  │ 데이터 풀 사용:                               │    │
│  │ • [✓] 경험 템플릿 풀                         │    │
│  │ • [✓] 아키타입 풀                            │    │
│  │                                              │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
│  ┌─ Agent 5: 이미지 생성 ───────────────────────┐    │
│  │ 활성화: [✓]                                  │    │
│  │ 모델: [Gemini Imagen 3.0] ▼                 │    │
│  │ 해상도: [256x512]                            │    │
│  │ 가이던스 스케일: [7.5]                        │    │
│  │                                              │    │
│  │ 품질 검증:                                    │    │
│  │ • [✓] 해상도 확인                            │    │
│  │ • [✓] 색상 수 제한 (32색)                    │    │
│  │ • [✓] 픽셀아트 스타일 검증                    │    │
│  │                                              │    │
│  │ 실패 시: [Fallback 프리셋 사용] ▼            │    │
│  │                                              │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
│  [테스트 실행]  [저장]                               │
└─────────────────────────────────────────────────────┘
```

### 5.2 품질 기준 설정

```
┌─────────────────────────────────────────────────────┐
│  품질 검증 기준                                       │
├─────────────────────────────────────────────────────┤
│                                                      │
│  📊 욕구 벡터 다양성                                  │
│  ───────────────────────────────────────────────    │
│  최소 욕구 개수: [2] (0.5 이상 강도)                 │
│  최대 욕구 개수: [4] (너무 많으면 1차원적)            │
│                                                      │
│  🔀 역설 포함                                        │
│  ───────────────────────────────────────────────    │
│  최소 역설 개수: [1]                                 │
│  역설 긴장도 최소: [0.5]                             │
│                                                      │
│  📝 프롬프트 구조                                     │
│  ───────────────────────────────────────────────    │
│  필수 섹션: [✓] WHY [✓] HOW [✓] WHAT                │
│  최소 길이: [1000] tokens                            │
│  최대 길이: [4000] tokens                            │
│                                                      │
│  🖼️ 이미지 품질                                      │
│  ───────────────────────────────────────────────    │
│  해상도: 256x512 (정확히 일치)                        │
│  색상 수: ≤ 40 (권장 32)                             │
│  픽셀아트 스타일: Sharp edge 비율 ≥ 70%              │
│  파일 크기: ≤ 200KB                                  │
│                                                      │
│  ⚠️ 기준 미달 시 동작:                               │
│  ( ) 경고만 표시                                     │
│  (●) 자동 재생성 (1회)                               │
│  ( ) 관리자 승인 요청                                │
│                                                      │
│  [저장]                                              │
└─────────────────────────────────────────────────────┘
```

---

## 6. 테스트 및 디버깅 도구

### 6.1 테스트 실행기

```
┌─────────────────────────────────────────────────────┐
│  에이전트 테스트 실행                                  │
├─────────────────────────────────────────────────────┤
│                                                      │
│  📥 테스트 입력 데이터                                │
│  ───────────────────────────────────────────────    │
│  ┌──────────────────────────────────────────┐      │
│  │ {                                        │      │
│  │   "userData": {                          │      │
│  │     "domains": [                         │      │
│  │       "github.com",                      │      │
│  │       "stackoverflow.com",               │      │
│  │       "reddit.com/r/gaming"              │      │
│  │     ],                                   │      │
│  │     "keywords": [                        │      │
│  │       "react hooks",                     │      │
│  │       "best indie games 2025"            │      │
│  │     ],                                   │      │
│  │     "interests": ["개발", "게임"],        │      │
│  │     "avoidTopics": ["정치"]              │      │
│  │   },                                     │      │
│  │   "meta": {                              │      │
│  │     "userId": "test_user_001",           │      │
│  │     "userName": "테스트유저",             │      │
│  │     "language": "ko"                     │      │
│  │   }                                      │      │
│  │ }                                        │      │
│  └──────────────────────────────────────────┘      │
│                                                      │
│  [샘플 데이터 로드] ▼  [파일에서 불러오기]            │
│                                                      │
│  [▶ 테스트 실행]                                    │
│                                                      │
│  📤 테스트 출력 결과                                  │
│  ───────────────────────────────────────────────    │
│  (실행 후 표시)                                      │
│                                                      │
└─────────────────────────────────────────────────────┘
```

### 6.2 결과 비교 도구

```
┌─────────────────────────────────────────────────────┐
│  A/B 테스트 - 템플릿 비교                             │
├─────────────────────────────────────────────────────┤
│                                                      │
│  템플릿 A: [Default v1.0] ▼                          │
│  템플릿 B: [Experimental v2.0] ▼                     │
│                                                      │
│  동일한 입력으로 테스트: [test_user_001]              │
│                                                      │
│  ┌─ 템플릿 A 결과 ──────┐  ┌─ 템플릿 B 결과 ──────┐│
│  │ 품질 점수: 87/100    │  │ 품질 점수: 92/100    ││
│  │ 욕구 다양성: ✓       │  │ 욕구 다양성: ✓       ││
│  │ 역설 포함: ✓         │  │ 역설 포함: ✓         ││
│  │ 생성 시간: 12.3초    │  │ 생성 시간: 14.1초    ││
│  │                      │  │                      ││
│  │ [상세 보기]          │  │ [상세 보기]          ││
│  └──────────────────────┘  └──────────────────────┘│
│                                                      │
│  [10회 반복 테스트]  [통계 비교]                     │
└─────────────────────────────────────────────────────┘
```

---

## 7. API 명세

### 7.1 에이전트 실행 API

```typescript
POST /api/v1/admin/agent/execute

Request:
{
  "input": AgentInput,      // I/O 스키마에 정의된 입력
  "config": {
    "templateId": "uuid",   // 사용할 템플릿
    "skipCache": false,     // 캐시 무시 여부
    "dryRun": false         // 테스트 모드 (DB 저장 안 함)
  }
}

Response:
{
  "success": true,
  "jobId": "job_12345",
  "status": "processing" | "completed" | "failed",
  "output": AgentOutput,  // 완료 시
  "logs": [...],
  "executionTime": 12.3
}
```

### 7.2 템플릿 관리 API

```typescript
// 템플릿 목록
GET /api/v1/admin/templates

// 템플릿 생성
POST /api/v1/admin/templates
{
  "name": "Default v1.0",
  "sections": { ... },
  "variables": [ ... ],
  "agentInstructions": "..."
}

// 템플릿 수정
PATCH /api/v1/admin/templates/:id

// 템플릿 삭제
DELETE /api/v1/admin/templates/:id
```

### 7.3 데이터 풀 관리 API

```typescript
// 경험 템플릿 목록
GET /api/v1/admin/data-pool/experiences

// 경험 템플릿 추가
POST /api/v1/admin/data-pool/experiences
{
  "category": "belonging",
  "title": "왕따 경험",
  "event": "...",
  "learnings": [...],
  "triggers": { ... }
}

// 아키타입 목록
GET /api/v1/admin/data-pool/archetypes

// 시각 요소 목록
GET /api/v1/admin/data-pool/visual-elements
```

---

## 8. 데이터베이스 스키마 (간소화)

### 8.1 prompt_templates (템플릿)

```sql
CREATE TABLE prompt_templates (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),

  name VARCHAR(255) NOT NULL,
  version VARCHAR(20) NOT NULL,

  -- 템플릿 섹션 (JSONB)
  sections JSONB NOT NULL,

  -- 변수 정의
  variables JSONB NOT NULL,

  -- 에이전트 지시사항
  agent_instructions TEXT,

  -- 메타
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

### 8.2 data_pool_experiences (경험 풀)

```sql
CREATE TABLE data_pool_experiences (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),

  category VARCHAR(50) NOT NULL,
  title VARCHAR(255) NOT NULL,
  event TEXT NOT NULL,
  age_range INT[2] NOT NULL,  -- [13, 15]
  learnings TEXT[] NOT NULL,

  -- 트리거 조건 (JSONB)
  triggers JSONB NOT NULL,

  created_at TIMESTAMP DEFAULT NOW()
);
```

### 8.3 data_pool_archetypes (아키타입 풀)

```sql
CREATE TABLE data_pool_archetypes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),

  name VARCHAR(100) NOT NULL,
  matching_needs TEXT[] NOT NULL,

  -- 시각적 요소 (JSONB)
  visual_elements JSONB NOT NULL,

  -- 대화 스타일 (JSONB)
  conversation_style JSONB NOT NULL,

  created_at TIMESTAMP DEFAULT NOW()
);
```

### 8.4 agent_execution_logs (실행 로그)

```sql
CREATE TABLE agent_execution_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),

  job_id VARCHAR(50) UNIQUE NOT NULL,
  user_id UUID REFERENCES users(id),

  -- 입출력
  input JSONB NOT NULL,
  output JSONB,

  -- 상태
  status VARCHAR(20) NOT NULL,  -- 'processing', 'completed', 'failed'
  error_message TEXT,

  -- 성능
  execution_time_ms INT,
  quality_score INT,

  -- 로그
  logs JSONB,

  started_at TIMESTAMP DEFAULT NOW(),
  completed_at TIMESTAMP
);

CREATE INDEX idx_agent_logs_job_id ON agent_execution_logs(job_id);
CREATE INDEX idx_agent_logs_status ON agent_execution_logs(status);
CREATE INDEX idx_agent_logs_user_id ON agent_execution_logs(user_id);
```

---

## 9. 구현 우선순위

### Phase 1: 핵심 인프라 (1주)
1. [ ] 데이터베이스 스키마 생성
2. [ ] 에이전트 I/O 스키마 정의
3. [ ] 기본 템플릿 1개 작성

### Phase 2: 에이전트 파이프라인 (2주)
1. [ ] Agent 1-5 구현
2. [ ] 데이터 풀 초기 데이터 생성
3. [ ] 품질 검증 로직

### Phase 3: 관리자 UI (1-2주)
1. [ ] 템플릿 편집기
2. [ ] 데이터 풀 관리 UI
3. [ ] 실행 모니터링 대시보드

### Phase 4: 테스트 & 최적화 (1주)
1. [ ] A/B 테스트 도구
2. [ ] 성능 최적화
3. [ ] 에러 핸들링

---

## 📝 요약

### 관리자 역할 (최소화)
✅ 시스템 프롬프트 **템플릿** 작성
✅ 에이전트 I/O **스키마** 정의
✅ **데이터 풀** 관리 (경험, 아키타입, 시각 요소)
✅ 에이전트 실행 **모니터링**

### 에이전트 역할 (완전 자동)
🤖 온보딩 데이터 → 욕구 벡터 분석
🤖 캐릭터 프로파일 자동 생성
🤖 시스템 프롬프트 조립
🤖 이미지 프롬프트 생성 + 이미지 생성
🤖 품질 검증 + 재시도

**핵심**: 관리자는 "틀"만 만들고, 에이전트가 "내용"을 채웁니다!
